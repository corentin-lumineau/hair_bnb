<!-- HAIR CARD -->
<%= link_to(hair_path(hair.id))do %>
  <div id="lien_out">
    <div class="hair_card mr-2" style="background-image: url('<%= cl_image_path hair.photo.key %>')">
      <i id="bg" class="fas fa-heart"></i>
      <i class="far fa-heart"></i>
    </div>
    <h3> <%= "#{hair.color.capitalize} | #{hair.nature.capitalize}" %> </h3>
  </div>
<% end %>
